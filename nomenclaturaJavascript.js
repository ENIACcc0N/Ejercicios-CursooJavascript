//  Notación
// ;
// .
// []
// {}

// ";" Delimita el final de una linea de código.
const b = 4;
let a = b + 5;
console.log(a);

// "." Se utiliza para los obejetos para accder a los atributos.
// Ejemplos: movil.anchura(se accede al atributo "anchura" del ojbeto "movil").

// [] Se utiliza para las listas, arreglos, arrays(también se utiliza para recorrer posiciones en el arrays).
const ar =[1, 2, 3, 4];
console.log(ar[3]);

// () Sirve para definir funciones.
function suma( a, b) {
    a + b
}
// {} Esto sirve para definir para curlie bracket, objetos, funciones, estructuras de control.

const movil = {
    altura: 10,
    anchura: 4.5,
}

if (true) {
    //Estructura alternativa
}

